#include "insertion_cell_sort.h"

void cell0(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell1(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell2(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell3(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell4(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell5(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell6(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell7(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell8(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell9(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell10(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell11(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell12(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell13(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell14(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell15(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell16(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell17(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell18(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell19(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell20(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell21(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell22(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell23(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell24(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell25(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell26(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell27(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell28(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell29(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell30(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void cell31(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	static DTYPE local = 0;
	DTYPE in_copy = in.read();
	if(in_copy > local) {
		out.write(local);
		local = in_copy;
	}
	else
	{
		out.write(in_copy);
	}
}

void insertion_cell_sort(hls::stream<DTYPE> & in, hls::stream<DTYPE> & out)
{
	#pragma HLS DATAFLOW
	hls::stream<DTYPE> out0("out0_stream");
	hls::stream<DTYPE> out1("out1_stream");
	hls::stream<DTYPE> out2("out2_stream");
	hls::stream<DTYPE> out3("out3_stream");
	hls::stream<DTYPE> out4("out4_stream");
	hls::stream<DTYPE> out5("out5_stream");
	hls::stream<DTYPE> out6("out6_stream");
	hls::stream<DTYPE> out7("out7_stream");
	hls::stream<DTYPE> out8("out8_stream");
	hls::stream<DTYPE> out9("out9_stream");
	hls::stream<DTYPE> out10("out10_stream");
	hls::stream<DTYPE> out11("out11_stream");
	hls::stream<DTYPE> out12("out12_stream");
	hls::stream<DTYPE> out13("out13_stream");
	hls::stream<DTYPE> out14("out14_stream");
	hls::stream<DTYPE> out15("out15_stream");
	hls::stream<DTYPE> out16("out16_stream");
	hls::stream<DTYPE> out17("out17_stream");
	hls::stream<DTYPE> out18("out18_stream");
	hls::stream<DTYPE> out19("out19_stream");
	hls::stream<DTYPE> out20("out20_stream");
	hls::stream<DTYPE> out21("out21_stream");
	hls::stream<DTYPE> out22("out22_stream");
	hls::stream<DTYPE> out23("out23_stream");
	hls::stream<DTYPE> out24("out24_stream");
	hls::stream<DTYPE> out25("out25_stream");
	hls::stream<DTYPE> out26("out26_stream");
	hls::stream<DTYPE> out27("out27_stream");
	hls::stream<DTYPE> out28("out28_stream");
	hls::stream<DTYPE> out29("out29_stream");
	hls::stream<DTYPE> out30("out30_stream");
	hls::stream<DTYPE> out31("out31_stream");
	hls::stream<DTYPE> out32("out32_stream");

	// Function calls;
	cell0(in, out1);
	cell1(out1, out2);
	cell2(out2, out3);
	cell3(out3, out4);
	cell4(out4, out5);
	cell5(out5, out6);
	cell6(out6, out7);
	cell7(out7, out8);
	cell8(out8, out9);
	cell9(out9, out10);
	cell10(out10, out11);
	cell11(out11, out12);
	cell12(out12, out13);
	cell13(out13, out14);
	cell14(out14, out15);
	cell15(out15, out);
    /*	cell16(out16, out17);
	cell17(out17, out18);
	cell18(out18, out19);
	cell19(out19, out20);
	cell20(out20, out21);
	cell21(out21, out22);
	cell22(out22, out23);
	cell23(out23, out24);
	cell24(out24, out25);
	cell25(out25, out26);
	cell26(out26, out27);
	cell27(out27, out28);
	cell28(out28, out29);
	cell29(out29, out30);
	cell30(out30, out31);
	cell31(out31, out);*/
}
